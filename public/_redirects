# ---------------------------------------------------------------------------
# 主机名收敛：GSC 中 www 与非 www 同时存在独立数据（重复内容）。
# 全站 canonical / hreflang / sitemap 均使用非 www，故将 www 301 归并过来。
# 规则按顺序匹配，先写精确项以避免 `www 根路径` 产生二次跳转链。
# ---------------------------------------------------------------------------
https://www.sulepagoda.org/ https://sulepagoda.org/my/ 301
https://www.sulepagoda.org/* https://sulepagoda.org/:splat 301

# ---------------------------------------------------------------------------
# 站点根路径 → 默认语种。
# 原来的 dist/index.html 是 <meta http-equiv="refresh" content="2;url=/my/">，
# 属于客户端软跳转（有 2 秒空白期、权重传递弱），这里改为服务端 301。
# ---------------------------------------------------------------------------
/ /my/ 301
